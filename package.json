{
  "scripts": {
    "netlify:status": "netlify status",
    "deploy:preview": "netlify deploy --dir=.",
    "deploy:prod:manual": "echo 'Production deploy requires explicit approval' && exit 1"
  },
  "dependencies": {
    "puppeteer": "^24.43.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
